//Logearse en MySQL
cd c:/xampp/mysql/bin
mysql -uroot -p
password: <mi password>

// Guardar mi trabajo en un archivo .txt
tee ruta\a\mi\archivo.txt;
<Hago todo mi trabajo>

// Listar todas mis bases de datos
show databases;

// Crear una base de datos
CREATE DATABASE <nombre de base de datos>;

// Usar una base de datos
USE <nombre de base datos>;

//Creacion de tablas
CREATE TABLE <nombre_tabla> (
id INT PRIMARY KEY AUTO_INCREMENT,
nombre VARCHAR(50) NOT NULL, // Not null = Tiene que tener datos
edad INT(3) // Sin not null = puede no tener datos
fecha DATE NOT NULL
);

// Como ver la estructura de una tabla
DESCRIBE <nombre de mi tabla>;

// Modificar campos de una tabla para añadir llave foranea
ALTER TABLE <nombre tabla> ADD CONSTRAINT <tabla_fk1> FOREIGN KEY (<campo tabla>) REFERENCES tabla2(<campo tabla2>) ON DELETE CASCADE ON UPDATE CASCADE;

// Modificar campos de una tabla
ALTER TABLE <nombre tabla> MODIFY COLUMN <columna> VARCHAR(50) NOT NULL

// Añadir campos a una tabla creada
ALTER TABLE <nombre tabla> ADD COLUMN fecha DATE;

// Insertar datos 1 a 1 en base de datos
INSERT INTO <nombre tabla>(campo1, campo2...) VALUES (valor1, valor2...);

// Insertar muchos datos a la vez
INSERT INTO <nombre tabla>(campo1, campo2...) VALUES
(valor1, valor2...),
(valor1, valor2...),
...
(valor1, valor2...);

// Ver TODOS los datos de una tabla de mi base de datos
SELECT * FROM <mi tabla>;

// Como ver ciertos datos de una tabla de la base de datos
SELECT campo1, campo2... FROM <mi tabla>;

// Como ver ciertos datos de una tabla y renombrarlos
SELECT campo1 as c1, campo2 as c2 FROM <tabla>;

// Seleccionar campos de varias tablas
SELECT <tabla1>.campo1, <tabla2>.campo2 FROM tabla1 INNER JOIN tabla2 ON tabla2.id = tabla1.id_tabla1
SELECT <tabla1>.campo1, <tabla2>.campo2 FROM tabla1, tabla2 WHERE tabla1.id = tabla2.id_tabla1;